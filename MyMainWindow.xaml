<Window x:Class="RevitMEPHoleManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RevitMEPHoleManager"
        Title="Автоматическая расстановка отверстий"
        Height="847" Width="760">
    <Window.Resources>
        <local:BoolToYesNoConverter x:Key="BoolToYesNoConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Автоматическая расстановка отверстий"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,10,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="10">
                    <TextBlock Text="Выбор семейства:" Margin="0,0,0,5"/>
                    <ComboBox x:Name="FamilyCombo" Height="25" IsEditable="False"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10">
                    <TextBlock Text="Зазор вокруг элемента (мм):" Margin="0,0,0,5"/>
                    <TextBox x:Name="ClearanceBox" Height="25"/>
                    <StackPanel Orientation="Horizontal" Margin="0,20,0,5">
                        <CheckBox x:Name="EnableMergeChk" Content="Объединять отверстия" VerticalAlignment="Center"/>
                        <TextBlock Text=" — макс. расстояние (мм):" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBox x:Name="MergeDistBox" Height="25" IsEnabled="{Binding IsChecked, ElementName=EnableMergeChk}"/>
                </StackPanel>
            </Grid>

            <DataGrid x:Name="StatsGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Хост" Binding="{Binding Host}" Width="110"/>
                    <DataGridTextColumn Header="Host Id" Binding="{Binding HostId}" Width="65"/>
                    <DataGridTextColumn Header="MEP Id" Binding="{Binding MepId}" Width="65"/>
                    <DataGridTextColumn Header="X, мм" Binding="{Binding CenterXmm, StringFormat=N0}" Width="70"/>
                    <DataGridTextColumn Header="Y, мм" Binding="{Binding CenterYmm, StringFormat=N0}" Width="70"/>
                    <DataGridTextColumn Header="Z, мм" Binding="{Binding CenterZmm, StringFormat=N0}" Width="70"/>
                    <DataGridTextColumn Header="Форма" Binding="{Binding Shape}" Width="75"/>
                    <DataGridTextColumn Header="Наклон" Binding="{Binding IsDiagonal, Converter={StaticResource BoolToYesNoConverter}}" Width="60"/>
                    <DataGridTextColumn Header="Ø / Шир., мм" Binding="{Binding ElemWidthMm, StringFormat=N0}" Width="85"/>
                    <DataGridTextColumn Header="Высота, мм" Binding="{Binding ElemHeightMm, StringFormat=N0}" Width="85"/>
                    <DataGridTextColumn Header="Расст., мм" Binding="{Binding GapMm, StringFormat=F0}" Width="70"/>
                    <DataGridTextColumn Header="Отв. Шир., мм" Binding="{Binding HoleWidthMm, StringFormat=N0}" Width="85"/>
                    <DataGridTextColumn Header="Отв. Выс., мм" Binding="{Binding HoleHeightMm, StringFormat=N0}" Width="85"/>
                    <DataGridTextColumn Header="Типоразмер" Binding="{Binding HoleTypeName}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <DataGrid x:Name="HostStatsGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Host Id" Binding="{Binding HostId}" Width="65"/>
                <DataGridTextColumn Header="Хост" Binding="{Binding HostName}" Width="110"/>
                <DataGridTextColumn Header="Круглых" Binding="{Binding Round}" Width="80"/>
                <DataGridTextColumn Header="Квадратных" Binding="{Binding Rect}" Width="95"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <TabControl Grid.Row="3" Margin="0,12,0,0" Height="220">
            <TabItem Header="Трубы проекта">
                <DataGrid x:Name="PipeGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Система" Binding="{Binding System}" Width="*"/>
                        <DataGridTextColumn Header="DN, мм" Binding="{Binding DN}" Width="80"/>
                        <DataGridTextColumn Header="Длина, мм" Binding="{Binding Length}" Width="90"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Лог расчёта">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="LogBox"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="12"
                             Background="Transparent"
                             BorderThickness="0"/>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Content="Старт" Width="120" Height="40" Background="LimeGreen" Foreground="White" HorizontalAlignment="Center" Click="StartButton_Click"/>
            <Button Content="?" Width="30" Height="30" FontSize="14" FontWeight="Bold" Background="#163D7A" Foreground="White" BorderBrush="{x:Null}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Padding="0" Grid.Column="1" Margin="0,0,-18,-17">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="22"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>
